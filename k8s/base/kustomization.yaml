resources: [deployment.yaml, service.yaml, hpa.yaml, pdb.yaml, networkpolicy.yaml]
labels:
  - pairs: { app.kubernetes.io/name: release-radar, app.kubernetes.io/managed-by: kustomize }
    includeSelectors: true
